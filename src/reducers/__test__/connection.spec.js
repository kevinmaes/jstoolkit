import { test, deepFreeze } from '../../spec_helper';
// import * as Types from '../../actions/Types';

test.skip('[reducer:connection:jid] default state', (t) => {
  // const result = jid(undefined, {});
  //
  // t.deepEqual(result, '', 'It should return the default state.');
  //
  // t.end();
});

test.skip('[reducer:connection:jid] RECEIVE_CONNECTION_STATUS - CONNECTED', (t) => {
  // const state = '1';
  // deepFreeze(state);
  //
  // const statuses = { CONNECTED: 'connected', DISCONNECTED: 'disconnected' };
  // const action = {
  //   type: Types.RECEIVE_CONNECTION_STATUS,
  //   status: statuses.CONNECTED,
  //   jid: 'somejid',
  //   statuses
  // };
  //
  // const result = jid(state, action);
  //
  // t.equal(result, action.jid,
  //   'It should be an object with the expected properties');

  // t.end();
});

test.skip('[reducer:connection:jid] unrecognized action', (t) => {
  // const state = {};
  // deepFreeze(state);
  //
  // const result = jid(state, {});
  //
  // t.equal(result, state, 'It should return the unchanged state.');
  //
  // t.end();
});
